// Esta función se ejecutará cuando se haga clic en el menú hamburguesa.
function toggleMenu() {
    const nav = document.querySelector('nav');
    nav.classList.toggle('active'); // Alterna la clase 'active' que muestra u oculta el menú
}
